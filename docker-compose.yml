nginx:
  build: nginx
  links:
    - openldap
  ports:
    - "80:80"
  volumes:
    - nginx/config/sites-enabled:/etc/nginx/sites-enabled
    - nginx/data/www:/var/www
openldap:
  build: openldap
  ports:
   - "389:389"
#fusiondirectory:
#  build: fusiondirectory
